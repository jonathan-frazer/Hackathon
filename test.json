{
    "database": "LibraryDB",
    "tables": [
      {
        "name": "authors",
        "columns": [
          {
            "name": "first_name",
            "type": "VARCHAR(50)",
            "constraints": ["NOT NULL"]
          },
          {
            "name": "last_name",
            "type": "VARCHAR(50)",
            "constraints": ["NOT NULL"]
          }
        ]
      }
    ]
  }